spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo_db
    username: dev2
    password: 456123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
    defaut-sheama: public
    clean-disabled: false
